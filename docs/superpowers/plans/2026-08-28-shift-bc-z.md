# shift 排班 B/C 班次 + Z/Z1 约束重构 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 shift 排班器中新增 B/C 班次，并把高强约束按家族拆分（D/D1 保留旧三条，Z/Z1 启用新四条）。

**Architecture:** 方案 A「班次家族化」——`utils.py` 定义 D_FAMILY/Z_FAMILY/三组均衡常量；硬约束全部收口在 `_can_assign_shift`（所有交换路径经 `_can_hold_shift` 自动继承）；新增 `shape_z_runs` 软整型步骤；校验器新增 check 16/17/18。规格见 `docs/superpowers/specs/2026-08-28-shift-bc-z-design.md`。

**Tech Stack:** Python 3.14 + openpyxl + Flask，纯 assert 测试（无 pytest）。

## Global Constraints

- 一律用 `.venv` 解释器并设 UTF-8：命令前 `$env:PYTHONIOENCODING="utf-8"`，解释器 `.\.venv\Scripts\python.exe`
- 测试为纯 assert 脚本，直接运行（非 pytest），文件名 `tests/test_*.py`，结尾 `if __name__ == "__main__":` 逐个调用并打印 `OK`
- `shift/` 使用扁平导入（`from utils import ...`），**禁止** `python -m`；测试通过 `tests/shift_test_utils.py` 统一把 `shift/` 加入 `sys.path`
- `shift/reader.py` 零改动
- `SHIFT_ORDER = ("D", "D1", "Z", "Z1", "C", "B", "A1", "A4", "A2", "A3")`（精确元组）
- z 参数默认 `z_min_consecutive=2`（软）、`z_max_consecutive=3`（硬）；入口校验 `1 ≤ z_min ≤ z_max`
- 三期员工只允许 A2/A3（COMFORT_SHIFTS 不变）；新人（系数<1）只禁 D/D1（HIGH_SHIFTS 不变）
- 提交遵循 Conventional Commits，中文描述；每任务一提交
- CI 在 Ubuntu 跑 `tests/test_*.py`，测试里路径一律 `os.path` 拼接，不用 Windows 字面量

---

### Task 1: utils 班次家族基础

**Files:**
- Modify: `shift/utils.py:6-16`（常量区）
- Create: `tests/test_shift_utils.py`

**Interfaces:**
- Produces: `SHIFT_ORDER`（含 C/B）、`D_FAMILY`、`Z_FAMILY`、`D_BALANCE_SHIFTS`、`Z_BALANCE_SHIFTS`、`A_BALANCE_SHIFTS`、`BALANCE_GROUPS`（三元组集合）；`HIGH_BALANCE_SHIFTS`/`SECONDARY_BALANCE_SHIFTS` 本任务暂留（Task 4 删除）

- [ ] **Step 1: 写失败测试**

创建 `tests/test_shift_utils.py`：

```python
# -*- coding: utf-8 -*-
"""shift 班次家族基础：B/C 班次与 D/Z 家族、三组均衡常量。"""
import os
import sys

_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _ROOT)
sys.path.insert(0, os.path.join(_ROOT, "shift"))

from utils import (  # noqa: E402
    A_CLASS_SHIFTS,
    BALANCE_GROUPS,
    COMFORT_SHIFTS,
    D_FAMILY,
    HIGH_LIMIT_SHIFTS,
    SHIFT_ORDER,
    Z_FAMILY,
    normalize_shift,
)


def test_shift_order_contains_bc():
    assert SHIFT_ORDER == ("D", "D1", "Z", "Z1", "C", "B", "A1", "A4", "A2", "A3")


def test_families():
    assert D_FAMILY == {"D", "D1"}
    assert Z_FAMILY == {"Z", "Z1"}
    assert HIGH_LIMIT_SHIFTS == {"D", "D1", "Z", "Z1"}


def test_bc_are_a_class_not_comfort():
    assert {"B", "C"} <= A_CLASS_SHIFTS
    assert "B" not in COMFORT_SHIFTS
    assert "C" not in COMFORT_SHIFTS


def test_balance_groups():
    assert BALANCE_GROUPS == ({"D", "D1"}, {"Z", "Z1"}, {"A1", "A4"})


def test_normalize_shift_bc():
    assert normalize_shift("b") == "B"
    assert normalize_shift("c") == "C"
    assert normalize_shift("B(8:30)") == "B"
    assert normalize_shift("a2") == "A2"
    assert normalize_shift("0") == "OFF"
    assert normalize_shift("奇怪的班") == "奇怪的班"


if __name__ == "__main__":
    test_shift_order_contains_bc()
    test_families()
    test_bc_are_a_class_not_comfort()
    test_balance_groups()
    test_normalize_shift_bc()
    print("test_shift_utils OK")
```

- [ ] **Step 2: 运行确认失败**

```powershell
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_utils.py
```

预期：`ImportError: cannot import name 'D_FAMILY'`。

- [ ] **Step 3: 修改 `shift/utils.py` 常量区**

将文件开头到 `REST_SHIFT = "OFF"` 的常量区整体替换为：

```python
SHIFT_ORDER = ("D", "D1", "Z", "Z1", "C", "B", "A1", "A4", "A2", "A3")
ALL_SHIFTS = set(SHIFT_ORDER) | {"OFF"}
HIGH_SHIFTS = {"D", "D1"}
D_FAMILY = HIGH_SHIFTS
SECONDARY_HIGH_SHIFTS = {"Z", "Z1"}
Z_FAMILY = SECONDARY_HIGH_SHIFTS
HIGH_LIMIT_SHIFTS = D_FAMILY | Z_FAMILY
A_CLASS_SHIFTS = {"A1", "A2", "A3", "A4", "B", "C"}
COMFORT_SHIFTS = {"A2", "A3"}
D_BALANCE_SHIFTS = set(D_FAMILY)
Z_BALANCE_SHIFTS = set(Z_FAMILY)
A_BALANCE_SHIFTS = {"A1", "A4"}
BALANCE_GROUPS = (D_BALANCE_SHIFTS, Z_BALANCE_SHIFTS, A_BALANCE_SHIFTS)
# 旧均衡组（Task 4 删除；删除前 scheduler/validators/writer 仍在引用）
HIGH_BALANCE_SHIFTS = {"D", "D1", "A1"}
SECONDARY_BALANCE_SHIFTS = {"Z", "Z1", "A4"}
WORK_SHIFTS = set(SHIFT_ORDER)
REST_SHIFT = "OFF"
```

`normalize_shift` 及其余函数不动（`"B"`/`"C"` 经 `text == shift` 命中，`"B(8:30)"` 经 `startswith(shift + "(")` 命中）。

- [ ] **Step 4: 运行确认通过**

```powershell
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_utils.py
```

预期：`test_shift_utils OK`。

- [ ] **Step 5: 回归全量 shift 相关旧测试（应无 shift 破坏，跑一遍全量确认基线）**

```powershell
$env:PYTHONIOENCODING="utf-8"; Get-ChildItem tests\test_*.py | ForEach-Object { .\.venv\Scripts\python.exe $_.FullName }
```

预期：全部 OK（旧测试不含 B/C 断言，`SHIFT_ORDER` 变化对零需求场景无行为影响）。

- [ ] **Step 6: 提交**

```powershell
git add shift/utils.py tests/test_shift_utils.py
git commit -m "feat(shift): 班次家族化基础——新增 B/C 班与 D/Z 家族、三组均衡常量"
```

---

### Task 2: SchedulerConfig z 参数 + 三个入口接线

**Files:**
- Modify: `shift/scheduler.py:22-31`（`SchedulerConfig`）
- Modify: `shift/app.py:96-116`（`/run` 参数解析，抽 `_scheduler_config_from`）
- Modify: `shift/main.py:11-19, 28-36`（常量与 config 构造）
- Modify: `shift/templates/index.html:212-215`（参数表单）
- Create: `tests/test_shift_scheduler.py`

**Interfaces:**
- Consumes: 无（独立配置项）
- Produces: `SchedulerConfig.z_min_consecutive: int = 2`、`SchedulerConfig.z_max_consecutive: int = 3`；`app._scheduler_config_from(form: dict) -> tuple[SchedulerConfig | None, str | None]`

- [ ] **Step 1: 写失败测试**

创建 `tests/test_shift_scheduler.py`：

```python
# -*- coding: utf-8 -*-
"""shift 排班器单元测试：配置、约束、整型步骤（随任务逐个扩充）。"""
import os
import sys

_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _ROOT)
sys.path.insert(0, os.path.join(_ROOT, "shift"))

from scheduler import SchedulerConfig  # noqa: E402


def test_z_config_defaults():
    cfg = SchedulerConfig()
    assert cfg.z_min_consecutive == 2
    assert cfg.z_max_consecutive == 3


def test_scheduler_config_from_form():
    from app import _scheduler_config_from
    cfg, err = _scheduler_config_from({})
    assert err is None
    assert cfg.z_min_consecutive == 2 and cfg.z_max_consecutive == 3

    cfg, err = _scheduler_config_from({"z_min_consecutive": "0"})
    assert cfg is None and "无效" in err

    cfg, err = _scheduler_config_from({"z_min_consecutive": "3", "z_max_consecutive": "2"})
    assert cfg is None and "无效" in err

    cfg, err = _scheduler_config_from({"z_min_consecutive": "2", "z_max_consecutive": "4"})
    assert err is None and cfg.z_max_consecutive == 4


if __name__ == "__main__":
    test_z_config_defaults()
    test_scheduler_config_from_form()
    print("test_shift_scheduler OK")
```

- [ ] **Step 2: 运行确认失败**

```powershell
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_scheduler.py
```

预期：`ImportError`/`AttributeError`（`z_min_consecutive`、`_scheduler_config_from` 不存在）。

- [ ] **Step 3: `SchedulerConfig` 加字段**

`shift/scheduler.py` 的 `SchedulerConfig` 末尾（`demand_tolerance` 之后）追加：

```python
    z_min_consecutive: int = 2
    z_max_consecutive: int = 3
```

- [ ] **Step 4: `app.py` 抽出参数解析并接入校验**

在 `app.py` 的 `_WARN_LABELS` 定义之后新增：

```python
def _scheduler_config_from(form) -> tuple[SchedulerConfig | None, str | None]:
    try:
        config = SchedulerConfig(
            preset_rest_days=int(form.get("preset_rest_days", 8)),
            max_consecutive_work_normal=int(form.get("max_consecutive_work_normal", 6)),
            max_consecutive_work_phase3=int(form.get("max_consecutive_work_phase3", 5)),
            max_consecutive_rest=int(form.get("max_consecutive_rest", 2)),
            min_work_days_between_rest_blocks=int(form.get("min_work_days_between_rest_blocks", 3)),
            max_high_consecutive=int(form.get("max_high_consecutive", 2)),
            balance_threshold=int(form.get("balance_threshold", 2)),
            z_min_consecutive=int(form.get("z_min_consecutive", 2)),
            z_max_consecutive=int(form.get("z_max_consecutive", 3)),
        )
    except (ValueError, TypeError) as e:
        return None, f"参数格式错误: {e}"
    if config.z_min_consecutive < 1 or config.z_max_consecutive < config.z_min_consecutive:
        return None, "Z/Z1 连排参数无效：需满足 1 ≤ 下限 ≤ 上限"
    return config, None
```

`/run` 视图中的 `try: config = SchedulerConfig(...) ... except` 块整体替换为：

```python
    config, error = _scheduler_config_from(request.form)
    if error:
        return jsonify({"error": error}), 400
```

- [ ] **Step 5: `main.py` 常量区与 config 构造**

常量区（`BALANCE_THRESHOLD = 2` 之后）加：

```python
Z_MIN_CONSECUTIVE = 2
Z_MAX_CONSECUTIVE = 3
```

`main()` 中 `config = SchedulerConfig(...)` 的 `balance_threshold=BALANCE_THRESHOLD,` 之后加：

```python
        z_min_consecutive=Z_MIN_CONSECUTIVE,
        z_max_consecutive=Z_MAX_CONSECUTIVE,
```

- [ ] **Step 6: `index.html` 表单**

`shift/templates/index.html` 中「最大连续高强度 (D/D1/Z/Z1)」一组（L212-215）：label 改为 `最大连续高强度 (D/D1)`；其后、`均衡阈值` 组之前插入两组：

```html
    <div class="form-group">
      <label>Z/Z1 连排下限</label>
      <input type="number" name="z_min_consecutive" value="2" min="1" max="14">
    </div>
    <div class="form-group">
      <label>Z/Z1 连排上限</label>
      <input type="number" name="z_max_consecutive" value="3" min="1" max="14">
    </div>
```

（前端 JS 按 `.form-grid input` 的 `name` 自动收集提交，无需改脚本。）

- [ ] **Step 7: 运行确认通过**

```powershell
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_scheduler.py
```

预期：`test_shift_scheduler OK`。

- [ ] **Step 8: 提交**

```powershell
git add shift/scheduler.py shift/app.py shift/main.py shift/templates/index.html tests/test_shift_scheduler.py
git commit -m "feat(shift): Z/Z1 连排上下限参数贯通 SchedulerConfig/CLI/Web"
```

---

### Task 3: 测试工具 + `_can_assign_shift` 家族分支

**Files:**
- Create: `tests/shift_test_utils.py`
- Modify: `shift/scheduler.py:734-762`（`_can_assign_shift`）、新增 `_d_run_count`/`_z_run_count`/`_is_d_family`/`_is_z_family`、删除 `_high_limited_count`（L1114-1115）
- Modify: `tests/test_shift_scheduler.py`（追加用例）

**Interfaces:**
- Consumes: Task 1 的 `D_FAMILY`/`Z_FAMILY`、Task 2 的 `SchedulerConfig.z_max_consecutive`
- Produces: `_can_assign_shift(schedule, employee, day_index, shift, config) -> bool`（家族分支版）；`_z_run_count(employee, day_index) -> int`；`_d_run_count(employee, day_index) -> int`；`_is_z_family(employee, day_index) -> bool`（Task 6/7 依赖）

- [ ] **Step 1: 创建共享测试工具 `tests/shift_test_utils.py`**

```python
# -*- coding: utf-8 -*-
"""shift 子项目测试工具：统一 sys.path 注入与内存 Schedule 构造。"""
import os
import sys

_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _ROOT)
sys.path.insert(0, os.path.join(_ROOT, "shift"))

from models import DailyDemand, Employee, Schedule, ShiftCell  # noqa: E402
from scheduler import SchedulerConfig  # noqa: E402
from utils import SHIFT_ORDER  # noqa: E402


def make_schedule(rows, history_days=0, lock_values=True):
    """构造内存 Schedule。

    rows: [(姓名, 班组, 系数, 人员类型, [班次值, ...])]，值 None 表示空格。
    lock_values=True 时非空格视为锁定（模拟用户已填数据）；传 False 便于测可动格。
    """
    employees = []
    n_days = len(rows[0][4])
    for i, (name, group, coeff, ptype, values) in enumerate(rows):
        cells = [
            ShiftCell(
                value=v,
                original_value=v,
                is_locked=(v is not None and lock_values),
                is_historical=idx < history_days,
                row=i + 2,
                column=5 + idx,
            )
            for idx, v in enumerate(values)
        ]
        employees.append(
            Employee(name=name, group=group, coefficient=coeff,
                     is_phase3="三期" in ptype, schedule=cells,
                     row_index=i + 2, person_type=ptype)
        )
    dates = list(range(n_days))
    demands = [DailyDemand(date=d, demand={s: 0.0 for s in SHIFT_ORDER + ("OFF",)})
               for d in dates]
    return Schedule(employees=employees, dates=dates, demands=demands,
                    workbook_path="", history_days=history_days)


def make_config(**kw):
    return SchedulerConfig(**kw)
```

- [ ] **Step 2: 在 `tests/test_shift_scheduler.py` 追加失败用例**

`from scheduler import SchedulerConfig` 之后追加导入：

```python
from shift_test_utils import make_config, make_schedule  # noqa: E402
from scheduler import _can_assign_shift  # noqa: E402
```

文件尾部（`if __name__` 之前）追加：

```python
def test_b_forbidden_after_d():
    s = make_schedule([("甲", "A", 1.0, "正式", ["D", None, None])])
    emp = s.employees[0]
    assert not _can_assign_shift(s, emp, 1, "B", make_config())
    assert _can_assign_shift(s, emp, 1, "A2", make_config())


def test_b_allowed_after_z1():
    s = make_schedule([("甲", "A", 1.0, "正式", ["Z1", None, None])])
    assert _can_assign_shift(s, s.employees[0], 1, "B", make_config())


def test_c_only_after_high():
    s = make_schedule([("甲", "A", 1.0, "正式", ["A2", "Z", None, None])])
    emp = s.employees[0]
    assert not _can_assign_shift(s, emp, 1, "C", make_config())  # 前日 A2
    assert not _can_assign_shift(s, emp, 3, "C", make_config())  # 前日空
    assert _can_assign_shift(s, emp, 2, "C", make_config())      # 前日 Z


def test_z_max_consecutive():
    s = make_schedule([("甲", "A", 1.0, "正式", [None, "Z", "Z", "Z", None])])
    emp = s.employees[0]
    assert not _can_assign_shift(s, emp, 4, "Z", make_config())  # 成 4 连
    assert not _can_assign_shift(s, emp, 0, "Z", make_config())  # 成 4 连（双向计数）
    s2 = make_schedule([("甲", "A", 1.0, "正式", [None, "Z", "Z", None, None])])
    assert _can_assign_shift(s2, s2.employees[0], 3, "Z", make_config())  # 恰 3 连


def test_z_next_day_not_off():
    s = make_schedule([("甲", "A", 1.0, "正式", ["Z", None, "OFF", None])])
    assert not _can_assign_shift(s, s.employees[0], 1, "Z", make_config())


def test_z_no_sandwich():
    # Z A2 ?：? 处排 Z 会形成 Z→A→Z
    s = make_schedule([("甲", "A", 1.0, "正式", ["Z", "A2", None, "A2", None])])
    emp = s.employees[0]
    assert not _can_assign_shift(s, emp, 2, "Z", make_config())
    # 对称：? A2 Z
    s2 = make_schedule([("甲", "A", 1.0, "正式", [None, "A2", "A2", "Z", None])])
    assert not _can_assign_shift(s2, s2.employees[0], 0, "Z", make_config())


def test_phase3_cannot_bc():
    s = make_schedule([("甲", "A", 1.0, "三期", ["A2", None, None])])
    emp = s.employees[0]
    assert not _can_assign_shift(s, emp, 1, "B", make_config())
    assert not _can_assign_shift(s, emp, 1, "C", make_config())
    assert _can_assign_shift(s, emp, 1, "A2", make_config())


def test_newbie_only_banned_from_d():
    s = make_schedule([("甲", "A", 0.8, "正式", ["A2", None, None])])
    emp = s.employees[0]
    assert not _can_assign_shift(s, emp, 1, "D", make_config())
    assert _can_assign_shift(s, emp, 1, "Z", make_config())
    assert _can_assign_shift(s, emp, 1, "B", make_config())


def test_z_run_counts_history():
    # 历史段 Z 计入连排：历史 3 连 Z 后，活跃段再排 Z = 4 连，超上限
    s = make_schedule([("甲", "A", 1.0, "正式", ["Z", "Z", "Z", None, None])], history_days=3)
    emp = s.employees[0]
    assert not _can_assign_shift(s, emp, 3, "Z", make_config())
    assert _can_assign_shift(s, emp, 3, "A2", make_config())


def test_fallback_never_bc_without_demand():
    from scheduler import _fallback_shift
    s = make_schedule([("甲", "A", 1.0, "正式",
                        ["OFF", None, "A2", "A2", "A2", "A2", "A2", "A2", "A2", "A2"])], lock_values=False)
    out = _fallback_shift(s, s.employees[0], 1, make_config())
    assert out not in ("B", "C"), "无需求缺口时兜底不得产出 B/C"
```

`__main__` 块逐个追加调用这 10 个函数。

- [ ] **Step 3: 运行确认失败**

```powershell
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_scheduler.py
```

预期：`test_b_forbidden_after_d` 失败（当前 B 不在约束里，`_can_assign_shift` 返回 True）。

- [ ] **Step 4: 修改 `shift/scheduler.py`**

4a. utils 导入行增加 `D_FAMILY`、`Z_FAMILY`（保持字母序插入）。

4b. `_can_assign_shift`（约 L734-762）整体替换为：

```python
def _can_assign_shift(
    schedule: Schedule,
    employee: Employee,
    day_index: int,
    shift: str,
    config: SchedulerConfig,
) -> bool:
    cell = employee.schedule[day_index]
    if cell.is_locked or cell.is_historical or not cell.is_blank:
        return False
    if employee.is_phase3 and shift not in COMFORT_SHIFTS:
        return False
    if employee.is_newbie and shift in HIGH_SHIFTS:
        return False
    if _consecutive_work_count(employee, day_index, shift) > _max_work_days(employee, config):
        return False
    if shift in D_FAMILY:
        if _d_run_count(employee, day_index) > config.max_high_consecutive:
            return False
        if _is_rest(employee, day_index + 1):
            return False
        if _is_a_class(employee, day_index - 1) and _is_d_family(employee, day_index - 2):
            return False
        if _is_a_class(employee, day_index + 1) and _is_d_family(employee, day_index + 2):
            return False
    if shift in Z_FAMILY:
        if _is_rest(employee, day_index + 1):
            return False
        if _z_run_count(employee, day_index) > config.z_max_consecutive:
            return False
        if _is_a_class(employee, day_index - 1) and _is_z_family(employee, day_index - 2):
            return False
        if _is_a_class(employee, day_index + 1) and _is_z_family(employee, day_index + 2):
            return False
    if shift in A_CLASS_SHIFTS:
        if _is_high_limited(employee, day_index - 1) and _is_high_limited(employee, day_index + 1):
            return False
    return True
```

4c. 删除 `_high_limited_count`（约 L1114-1115），在原位置附近新增四个助手：

```python
def _d_run_count(employee: Employee, day_index: int) -> int:
    return 1 + _same_group_count(employee, day_index - 1, -1, D_FAMILY) + _same_group_count(employee, day_index + 1, 1, D_FAMILY)


def _z_run_count(employee: Employee, day_index: int) -> int:
    return 1 + _same_group_count(employee, day_index - 1, -1, Z_FAMILY) + _same_group_count(employee, day_index + 1, 1, Z_FAMILY)


def _is_d_family(employee: Employee, day_index: int) -> bool:
    return 0 <= day_index < len(employee.schedule) and employee.schedule[day_index].base_shift in D_FAMILY


def _is_z_family(employee: Employee, day_index: int) -> bool:
    return 0 <= day_index < len(employee.schedule) and employee.schedule[day_index].base_shift in Z_FAMILY
```

- [ ] **Step 5: 全局搜索确认无残留引用**

用 grep 工具搜 `_high_limited_count`：预期 0 命中。

- [ ] **Step 6: 运行确认通过 + 全量回归**

```powershell
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_scheduler.py
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_utils.py
```

预期：两个 OK。

- [ ] **Step 7: 提交**

```powershell
git add shift/scheduler.py tests/shift_test_utils.py tests/test_shift_scheduler.py
git commit -m "feat(shift): _can_assign_shift 家族分支——D 族旧三条、Z 族连排上限/贴休/禁夹心"
```

---

### Task 4: 均衡三组重构

**Files:**
- Modify: `shift/utils.py`（删 `HIGH_BALANCE_SHIFTS`/`SECONDARY_BALANCE_SHIFTS` 两行）
- Modify: `shift/scheduler.py`（`_shift_key` 约 L968-982、`redistribute_balance` 约 L538-595、导入行）
- Modify: `shift/validators.py`（`_check_balance` 约 L199-211、导入行）
- Modify: `shift/writer.py`（`_write_stats` 员工统计列 约 L150-173、导入行）
- Create: `tests/test_shift_writer.py`

**Interfaces:**
- Consumes: Task 1 的 `BALANCE_GROUPS`/`D_BALANCE_SHIFTS`/`Z_BALANCE_SHIFTS`/`A_BALANCE_SHIFTS`
- Produces: `_shift_key` 按组取计数；`redistribute_balance` 遍历三组；check 10 按组报警（仅统计非三期）；统计表三列 `D/D1 均衡`/`Z/Z1 均衡`/`A1/A4 均衡`

- [ ] **Step 1: 写失败测试**

创建 `tests/test_shift_writer.py`：

```python
# -*- coding: utf-8 -*-
"""shift 输出层测试：统计表列。"""
import os
import sys

_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _ROOT)
sys.path.insert(0, os.path.join(_ROOT, "shift"))

from openpyxl import Workbook  # noqa: E402

from shift_test_utils import make_config, make_schedule  # noqa: E402
from writer import _write_stats  # noqa: E402


def test_stats_employee_columns():
    s = make_schedule([
        ("甲", "A", 1.0, "正式", ["D", "A2", "A2", "A2", "OFF"]),
        ("乙", "A", 1.0, "正式", ["Z", "Z", "A1", "A4", "OFF"]),
    ])
    wb = Workbook()
    _write_stats(wb.active, s)
    ws = wb.active
    header_row = next(r for r in range(1, ws.max_row + 1) if ws.cell(r, 1).value == "姓名")
    headers = [ws.cell(header_row, c).value for c in range(1, 8)]
    assert headers == ["姓名", "班组", "D/D1 均衡", "Z/Z1 均衡", "A1/A4 均衡", "休息天数", "连续双休次数"]


def test_stats_values():
    s = make_schedule([
        ("甲", "A", 1.0, "正式", ["D", "D1", "Z", "A1", "A4"]),
        ("乙", "A", 1.0, "正式", ["A2", "A2", "A3", "OFF", "OFF"]),
    ], lock_values=False)
    # 甲: D/D1=2, Z/Z1=1, A1/A4=2；乙: 全 0
    wb = Workbook()
    _write_stats(wb.active, s)
    ws = wb.active
    header_row = next(r for r in range(1, ws.max_row + 1) if ws.cell(r, 1).value == "姓名")
    row_jia = [ws.cell(header_row + 1, c).value for c in range(3, 6)]
    row_yi = [ws.cell(header_row + 2, c).value for c in range(3, 6)]
    assert row_jia == [2, 1, 2]
    assert row_yi == [0, 0, 0]


if __name__ == "__main__":
    test_stats_employee_columns()
    test_stats_values()
    print("test_shift_writer OK")
```

- [ ] **Step 2: 运行确认失败**

```powershell
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_writer.py
```

预期：headers 断言失败（当前为「高强均衡(D/D1/A1)」「次高强均衡(Z/Z1/A4)」两列）。

- [ ] **Step 3: `utils.py` 删除旧组**

删除这两行：

```python
HIGH_BALANCE_SHIFTS = {"D", "D1", "A1"}
SECONDARY_BALANCE_SHIFTS = {"Z", "Z1", "A4"}
```

- [ ] **Step 4: `scheduler.py`**

4a. 导入行：去掉 `HIGH_BALANCE_SHIFTS`、`SECONDARY_BALANCE_SHIFTS`，加入 `A_BALANCE_SHIFTS` 不需要——只需 `BALANCE_GROUPS`（`D_BALANCE_SHIFTS` 等不直接用）。导入区最终含：`A_CLASS_SHIFTS, BALANCE_GROUPS, COMFORT_SHIFTS, D_FAMILY, HIGH_LIMIT_SHIFTS, HIGH_SHIFTS, REST_SHIFT, SHIFT_ORDER, WORK_SHIFTS, Z_FAMILY, almost_met, date_label`。

4b. `_shift_key` 替换为：

```python
def _shift_key(schedule: Schedule, employee: Employee, day_index: int, shift: str) -> tuple:
    balance = 0
    for group in BALANCE_GROUPS:
        if shift in group:
            balance = _active_count_any(employee, group, schedule)
            break
    return (
        balance,
        _active_count(employee, shift, schedule),
        _active_count_any(employee, HIGH_LIMIT_SHIFTS, schedule),
        _work_streak_before(employee, day_index),
        employee.group,
        employee.name,
    )
```

4c. `redistribute_balance`：首行 `for group in (HIGH_BALANCE_SHIFTS, SECONDARY_BALANCE_SHIFTS):` 改为 `for group in BALANCE_GROUPS:`；docstring 同步改为「均匀 D/D1、Z/Z1、A1/A4 三组班在合格(非三期)员工间的分布」。函数体其余不动（receivers 已排除三期）。

- [ ] **Step 5: `validators.py` `_check_balance` 替换**

导入区：去掉 `HIGH_BALANCE_SHIFTS`、`SECONDARY_BALANCE_SHIFTS`，加入 `A_BALANCE_SHIFTS, D_BALANCE_SHIFTS, Z_BALANCE_SHIFTS`。函数替换为：

```python
def _check_balance(schedule: Schedule, config: SchedulerConfig) -> list[Warning]:
    warnings = []
    groups = [
        ("D/D1", D_BALANCE_SHIFTS),
        ("Z/Z1", Z_BALANCE_SHIFTS),
        ("A1/A4", A_BALANCE_SHIFTS),
    ]
    employees = [e for e in schedule.employees if not e.is_phase3]
    for label, group in groups:
        counts = [_count_any(employee, group, schedule) for employee in employees]
        if counts and max(counts) - min(counts) > config.balance_threshold:
            warnings.append(
                Warning("10", "WARN", f"{label} 均衡 max-min={max(counts) - min(counts)}，超过 {config.balance_threshold}")
            )
    return warnings
```

（注意：均衡统计范围改为非三期员工——三期本就不承接这三组班，避免其恒 0 计数把 min 压到 0 造成误报。）

- [ ] **Step 6: `writer.py` 员工统计三列**

导入区：去掉 `HIGH_BALANCE_SHIFTS`、`SECONDARY_BALANCE_SHIFTS`，加入 `A_BALANCE_SHIFTS, D_BALANCE_SHIFTS, Z_BALANCE_SHIFTS`。`_write_stats` 中员工统计段（原 L150-173）替换为：

```python
    row += 2
    row = _section(ws, row, "员工统计", header_fill, header_font)
    ws.append(["姓名", "班组", "D/D1 均衡", "Z/Z1 均衡", "A1/A4 均衡", "休息天数", "连续双休次数"])
    _style_header(ws[row])
    row += 1
    for employee in schedule.employees:
        d_count = 0
        z_count = 0
        a_count = 0
        rest_days = 0
        double_rests = 0
        prev_rest = False
        for idx in schedule.active_indexes:
            base = employee.schedule[idx].base_shift
            if base in D_BALANCE_SHIFTS:
                d_count += 1
            if base in Z_BALANCE_SHIFTS:
                z_count += 1
            if base in A_BALANCE_SHIFTS:
                a_count += 1
            is_rest = base == "OFF"
            if is_rest:
                rest_days += 1
            if is_rest and prev_rest:
                double_rests += 1
            prev_rest = is_rest
        ws.append([employee.name, employee.group, d_count, z_count, a_count, rest_days, double_rests])
        row += 1
```

- [ ] **Step 7: grep 确认无残留**

用 grep 工具搜 `HIGH_BALANCE_SHIFTS|SECONDARY_BALANCE_SHIFTS`：预期 0 命中。

- [ ] **Step 8: 运行 writer 测试 + 追加均衡行为测试**

`tests/test_shift_scheduler.py` 追加：

```python
def test_redistribute_balance_z_group():
    from scheduler import redistribute_balance
    s = make_schedule([
        ("甲", "A", 1.0, "正式", ["Z", "Z", "Z"] + ["A2"] * 7),
        ("乙", "A", 1.0, "正式", ["A2"] * 10),
    ], lock_values=False)
    redistribute_balance(s, make_config(balance_threshold=2))
    bases0 = [c.base_shift for c in s.employees[0].schedule]
    bases1 = [c.base_shift for c in s.employees[1].schedule]
    n0 = sum(1 for b in bases0 if b in ("Z", "Z1"))
    n1 = sum(1 for b in bases1 if b in ("Z", "Z1"))
    assert n0 - n1 <= 2
    assert bases1[0] == "Z" and bases0[0] == "A2"
```

`tests/test_shift_validators.py` 新建：

```python
# -*- coding: utf-8 -*-
"""shift 校验器测试（随任务扩充）。"""
import os
import sys

_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _ROOT)
sys.path.insert(0, os.path.join(_ROOT, "shift"))

from shift_test_utils import make_config, make_schedule  # noqa: E402
from validators import _check_balance  # noqa: E402


def test_check_balance_z_group_non_phase3():
    s = make_schedule([
        ("甲", "A", 1.0, "正式", ["Z", "Z", "Z"] + ["A2"] * 7),
        ("乙", "A", 1.0, "正式", ["A2"] * 10),
        ("丙", "A", 1.0, "三期", ["A2"] * 10),
    ])
    warns = [w for w in _check_balance(s, make_config(balance_threshold=2)) if "Z/Z1" in w.message]
    assert warns, "Z/Z1 计数差 3 > 2 应报警"


if __name__ == "__main__":
    test_check_balance_z_group_non_phase3()
    print("test_shift_validators OK")
```

运行三个测试文件，预期全 OK。

- [ ] **Step 9: 提交**

```powershell
git add shift/utils.py shift/scheduler.py shift/validators.py shift/writer.py tests/test_shift_scheduler.py tests/test_shift_validators.py tests/test_shift_writer.py
git commit -m "refactor(shift): 均衡组家族化——D/D1、Z/Z1、A1/A4 三组独立均衡，check10 仅统计非三期"
```

---

### Task 5: `cluster_high_pairs` 改管 D 族

**Files:**
- Modify: `shift/scheduler.py:417-453`（`cluster_high_pairs`）、`shift/scheduler.py:481-510`（`_move_high_in_row`）
- Modify: `tests/test_shift_scheduler.py`（追加用例）

**Interfaces:**
- Consumes: Task 1 的 `D_FAMILY`
- Produces: `cluster_high_pairs(schedule, config)` 只聚合 D/D1（Z 块归 Task 7 的 `shape_z_runs` 管）

- [ ] **Step 1: 追加失败测试**

`tests/test_shift_scheduler.py` 追加（导入行加 `from scheduler import cluster_high_pairs`）：

```python
def test_cluster_high_pairs_d_family_only():
    from scheduler import cluster_high_pairs
    # 块内 D@1 与 D1@3 不相邻 → 应聚成 D D1 相连；Z/Z1 不参与计数
    s = make_schedule([("甲", "A", 1.0, "正式",
                        ["OFF", "D", "A2", "D1", "A2", "OFF", "A2", "A2"])], lock_values=False)
    cluster_high_pairs(s, make_config())
    bases = [c.base_shift for c in s.employees[0].schedule]
    assert bases[1:3] == ["D", "D1"], bases
    assert bases[3] == "A2"
```

`__main__` 追加调用。

- [ ] **Step 2: 运行确认失败**

```powershell
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_scheduler.py
```

预期：`test_cluster_high_pairs_d_family_only` 失败——当前按 `HIGH_LIMIT_SHIFTS` 计数，块内高强为 D/Z1/D1 = 3 个 ≠ 2，不做聚合，`bases[1:3]` 仍为 `["D", "A2"]`。

- [ ] **Step 3: 修改**

`cluster_high_pairs`：docstring 的「高强班(D/D1/Z/Z1)」改为「D 族班(D/D1)」；函数体内两处 `HIGH_LIMIT_SHIFTS` 改为 `D_FAMILY`（highs 列表推导与「other」推导）。

`_move_high_in_row`：`if high not in HIGH_LIMIT_SHIFTS or x not in WORK_SHIFTS:` 改为 `if high not in D_FAMILY or x not in WORK_SHIFTS:`；docstring 中「高强」改为「D 族班」。

- [ ] **Step 4: 运行确认通过**

```powershell
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_scheduler.py
```

预期：全部 OK。

- [ ] **Step 5: 提交**

```powershell
git add shift/scheduler.py tests/test_shift_scheduler.py
git commit -m "refactor(shift): cluster_high_pairs 只聚合 D 族，Z 块交由 shape_z_runs"
```

---

### Task 6: 校验器——check 08 改 D 族 + 新增 16/17/18 + Web 提醒标签

**Files:**
- Modify: `shift/validators.py`（`validate_schedule` L20-38、`_check_high_streaks` L171-180、新增三个检查函数与 `_pair_severity`）
- Modify: `shift/app.py:42-50`（`_WARN_LABELS`）
- Modify: `tests/test_shift_validators.py`（追加用例）

**Interfaces:**
- Consumes: Task 3 的 `_is_z_family`（不需要——校验独立实现）、Task 4 的导入基础；`scheduler._work_blocks_between_rests`
- Produces: `_check_b_predecessor(schedule)`、`_check_c_placement(schedule)`、`_check_z_blocks(schedule, config)`；check id 16/17/18

- [ ] **Step 1: 追加失败测试**

`tests/test_shift_validators.py`：导入行改为

```python
from validators import _check_b_predecessor, _check_balance, _check_c_placement, _check_high_streaks, _check_z_blocks  # noqa: E402
```

追加用例：

```python
def test_check_b_predecessor():
    s = make_schedule([("甲", "A", 1.0, "正式", ["D", "B", "A2"])], lock_values=False)
    ws = _check_b_predecessor(s)
    assert len(ws) == 1 and ws[0].severity == "ERROR" and ws[0].check_id == "16"

    s2 = make_schedule([("甲", "A", 1.0, "正式", ["D", "B", "A2"])])  # 默认锁定 → WARN
    ws2 = _check_b_predecessor(s2)
    assert ws2 and ws2[0].severity == "WARN"

    s3 = make_schedule([("甲", "A", 1.0, "正式", ["Z1", "B", "A2"])], lock_values=False)
    assert _check_b_predecessor(s3) == []  # Z1 后可 B


def test_check_c_placement():
    s = make_schedule([("甲", "A", 1.0, "正式", ["A2", "C", "A2"])], lock_values=False)
    ws = _check_c_placement(s)
    assert len(ws) == 1 and ws[0].severity == "ERROR" and ws[0].check_id == "17"

    s2 = make_schedule([("甲", "A", 1.0, "正式", ["Z1", "C", "A2"])], lock_values=False)
    assert _check_c_placement(s2) == []  # Z1 后可 C


def test_check_z_blocks_over_max():
    s = make_schedule([("甲", "A", 1.0, "正式",
                        ["Z", "Z", "Z", "Z", "A2", "A2", "A2", "A2", "OFF", "A2"])], lock_values=False)
    msgs = [w.message for w in _check_z_blocks(s, make_config())]
    assert any("超过上限" in m for m in msgs)


def test_check_z_blocks_short():
    s = make_schedule([("甲", "A", 1.0, "正式",
                        ["OFF", "Z", "A2", "A2", "A2", "A2", "A2", "A2", "OFF", "A2"])], lock_values=False)
    msgs = [w.message for w in _check_z_blocks(s, make_config())]
    assert any("少于下限" in m for m in msgs)


def test_check_z_blocks_multi_run():
    s = make_schedule([("甲", "A", 1.0, "正式",
                        ["OFF", "Z", "Z", "A2", "A2", "Z", "Z", "A2", "OFF", "A2"])], lock_values=False)
    msgs = [w.message for w in _check_z_blocks(s, make_config())]
    assert any("多个 Z/Z1 块" in m for m in msgs)


def test_check_high_streaks_d_only():
    s = make_schedule([("甲", "A", 1.0, "正式",
                        ["Z", "Z", "Z", "A2", "A2", "A2", "A2", "A2", "A2", "A2"])], lock_values=False)
    assert _check_high_streaks(s, make_config()) == []  # Z 3 连不再触发 08

    s2 = make_schedule([("甲", "A", 1.0, "正式",
                         ["D", "D", "D", "A2", "A2", "A2", "A2", "A2", "A2", "A2"])], lock_values=False)
    assert any("D/D1" in w.message for w in _check_high_streaks(s2, make_config()))
```

`__main__` 追加调用。

- [ ] **Step 2: 运行确认失败**

```powershell
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_validators.py
```

预期：`ImportError: cannot import name '_check_b_predecessor'`。

- [ ] **Step 3: 修改 `shift/validators.py`**

3a. 导入区：`from utils import ...` 增加 `D_FAMILY, Z_FAMILY`；`from scheduler import SchedulerConfig` 行改为

```python
from scheduler import SchedulerConfig, _work_blocks_between_rests
```

3b. `validate_schedule` 中 `warnings.extend(_check_high_streaks(schedule, config))` 之后追加三行：

```python
    warnings.extend(_check_b_predecessor(schedule))
    warnings.extend(_check_c_placement(schedule))
    warnings.extend(_check_z_blocks(schedule, config))
```

3c. `_check_high_streaks`：`_streaks(employee, HIGH_LIMIT_SHIFTS)` 改为 `_streaks(employee, D_FAMILY)`；消息改为 `f"D/D1 连续 {streak} 天，超过 {config.max_high_consecutive} 天"`。

3d. 文件末尾（`_count_any` 之前）新增：

```python
def _pair_severity(employee: Employee, first_idx: int, second_idx: int) -> str:
    for i in (first_idx, second_idx):
        cell = employee.schedule[i]
        if cell.is_locked or cell.is_historical:
            return "WARN"
    return "ERROR"


def _check_b_predecessor(schedule: Schedule) -> list[Warning]:
    warnings = []
    for employee in schedule.employees:
        for idx in schedule.active_indexes:
            if idx <= 0 or employee.schedule[idx].base_shift != "B":
                continue
            prev = employee.schedule[idx - 1].base_shift
            if prev not in ("D", "D1", "Z"):
                continue
            warnings.append(
                Warning("16", _pair_severity(employee, idx - 1, idx),
                        f"B 班前一日为 {prev}，不允许（仅 D/D1/Z 后可排 B，Z1 后可排）",
                        employee.name, schedule.dates[idx])
            )
    return warnings


def _check_c_placement(schedule: Schedule) -> list[Warning]:
    warnings = []
    for employee in schedule.employees:
        for idx in schedule.active_indexes:
            if idx <= 0 or employee.schedule[idx].base_shift != "C":
                continue
            prev = employee.schedule[idx - 1].base_shift
            if prev in HIGH_LIMIT_SHIFTS:
                continue
            warnings.append(
                Warning("17", _pair_severity(employee, idx - 1, idx),
                        f"C 班前一日为 {prev or '空'}，C 只能排在 D/D1/Z/Z1 之后",
                        employee.name, schedule.dates[idx])
            )
    return warnings


def _check_z_blocks(schedule: Schedule, config: SchedulerConfig) -> list[Warning]:
    warnings = []
    for employee in schedule.employees:
        # ① 连排超硬上限
        for start, end, streak in _streaks(employee, Z_FAMILY):
            if streak > config.z_max_consecutive:
                warnings.append(
                    Warning("18", _streak_severity(employee, start, end),
                            f"Z/Z1 连续 {streak} 天，超过上限 {config.z_max_consecutive} 天",
                            employee.name, schedule.dates[end])
                )
        # ③ 块长短于软下限（完全落在历史段的块跳过）
        for start, end, _ in _streaks(employee, Z_FAMILY):
            length = end - start + 1
            if length < config.z_min_consecutive and end >= schedule.work_start_index:
                warnings.append(
                    Warning("18", "WARN",
                            f"Z/Z1 块仅 {length} 天，少于下限 {config.z_min_consecutive} 天",
                            employee.name, schedule.dates[start])
                )
        # ② 同一工作块内多个 Z 块（Z→A→Z）
        for wb_start, wb_end in _work_blocks_between_rests(employee):
            runs = [(s, e) for s, e, _ in _streaks(employee, Z_FAMILY) if wb_start <= s and e <= wb_end]
            if len(runs) > 1:
                warnings.append(
                    Warning("18", _multi_block_severity(employee, runs[0], runs[-1]),
                            "两次休息之间出现多个 Z/Z1 块（Z→A→Z）",
                            employee.name, schedule.dates[runs[1][0]])
                )
    return warnings
```

- [ ] **Step 4: `app.py` `_WARN_LABELS` 追加**

```python
    "16": "B班前置",
    "17": "C班位置",
    "18": "Z/Z1块形状",
```

- [ ] **Step 5: 运行确认通过**

```powershell
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_validators.py
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_scheduler.py
```

预期：两个 OK。

- [ ] **Step 6: 提交**

```powershell
git add shift/validators.py shift/app.py tests/test_shift_validators.py
git commit -m "feat(shift): 新增 check16/17/18（B 前置、C 位置、Z 块形状），check08 收窄为 D 族"
```

---

### Task 7: `shape_z_runs` 新流水线步骤

**Files:**
- Modify: `shift/scheduler.py`（`run_scheduler` L34-45、新增 `shape_z_runs` 与 4 个助手）
- Modify: `tests/test_shift_scheduler.py`（追加用例）

**Interfaces:**
- Consumes: `_can_assign_shift`（Task 3）、`_demand_ok_removing`（已有）、`_work_blocks_between_rests`/`_group_streaks`（已有）
- Produces: `shape_z_runs(schedule, config) -> None`；`_swap_pair(schedule, employee, i, j, config) -> bool`；`_z_blocks(employee) -> list[tuple[int, int]]`

- [ ] **Step 1: 追加失败测试**

`tests/test_shift_scheduler.py` 追加（导入行加 `from scheduler import shape_z_runs, _swap_pair`）：

```python
def test_shape_z_extends_orphan():
    s = make_schedule([("甲", "A", 1.0, "正式",
                        ["OFF", "Z", "A2", "A2", "A2", "A2", "A2", "A2", "OFF", "A2"])], lock_values=False)
    shape_z_runs(s, make_config())
    bases = [c.base_shift for c in s.employees[0].schedule]
    assert bases[1:3] == ["Z", "Z"], bases


def test_shape_z_respects_zmax():
    s = make_schedule([("甲", "A", 1.0, "正式",
                        ["OFF", "Z", "Z", "Z", "A2", "A2", "A2", "A2", "OFF", "A2"])], lock_values=False)
    shape_z_runs(s, make_config())
    bases = [c.base_shift for c in s.employees[0].schedule]
    assert bases[4] == "A2", "已达上限不得再扩"


def test_shape_z_merges_runs():
    s = make_schedule([("甲", "A", 1.0, "正式",
                        ["OFF", "Z", "Z", "A2", "Z", "Z", "A2", "A2", "OFF", "A2"])], lock_values=False)
    shape_z_runs(s, make_config(z_max_consecutive=5))
    bases = [c.base_shift for c in s.employees[0].schedule]
    assert bases[1:6] == ["Z"] * 5, bases


def test_shape_z_pulls_to_off():
    s = make_schedule([("甲", "A", 1.0, "正式",
                        ["OFF", "A2", "Z", "Z", "A2", "A2", "A2", "A2", "OFF", "A2"])], lock_values=False)
    shape_z_runs(s, make_config())
    bases = [c.base_shift for c in s.employees[0].schedule]
    assert bases[1] == "Z", bases


def test_swap_pair():
    s = make_schedule([("甲", "A", 1.0, "正式",
                        ["OFF", "A2", "Z", "Z", "A2", "OFF", "A2", "A2", "A2", "A2"])], lock_values=False)
    assert _swap_pair(s, s.employees[0], 1, 2, make_config())
    bases = [c.base_shift for c in s.employees[0].schedule]
    assert bases[1:3] == ["Z", "A2"]
```

`__main__` 追加调用。

- [ ] **Step 2: 运行确认失败**

```powershell
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_scheduler.py
```

预期：`ImportError: cannot import name 'shape_z_runs'`。

- [ ] **Step 3: `shift/scheduler.py` 新增代码**

3a. `run_scheduler` 中 `repair_employee_rest_count(schedule, config)` 与 `cluster_high_pairs(schedule, config)` 之间插入一行：

```python
    shape_z_runs(schedule, config)
```

3b. 在 `cluster_high_pairs` 定义之前新增：

```python
def shape_z_runs(schedule: Schedule, config: SchedulerConfig) -> None:
    """Z/Z1 块整型（软约束）：短块扩长、同工作块内合并、块尽量贴前一个 OFF。

    行内改动均受需求容差与 `_can_assign_shift` 硬约束保护。
    """
    for employee in schedule.employees:
        _extend_short_z_blocks(schedule, employee, config)
        _merge_z_blocks(schedule, employee, config)
        _pull_z_blocks_to_off(schedule, employee, config)


def _z_blocks(employee: Employee) -> list[tuple[int, int]]:
    return [(start, end) for start, end, _ in _group_streaks(employee, Z_FAMILY)]


def _extend_short_z_blocks(schedule: Schedule, employee: Employee, config: SchedulerConfig) -> None:
    for start, end in list(_z_blocks(employee)):
        if end - start + 1 >= config.z_min_consecutive:
            continue
        for day_to in (start - 1, end + 1):
            if _convert_to_z(schedule, employee, day_to, config):
                break


def _merge_z_blocks(schedule: Schedule, employee: Employee, config: SchedulerConfig) -> None:
    for wb_start, wb_end in list(_work_blocks_between_rests(employee)):
        runs = [r for r in _z_blocks(employee) if wb_start <= r[0] and r[1] <= wb_end]
        if len(runs) < 2:
            continue
        gap = list(range(runs[0][1] + 1, runs[1][0]))
        new_len = (runs[1][1] - runs[0][0] + 1) + len(gap)
        if new_len > config.z_max_consecutive or not gap:
            continue
        if all(_can_place_z(schedule, employee, i, config) for i in gap):
            for i in gap:
                _convert_to_z(schedule, employee, i, config)


def _pull_z_blocks_to_off(schedule: Schedule, employee: Employee, config: SchedulerConfig) -> None:
    for wb_start, wb_end in _work_blocks_between_rests(employee):
        for z_start, _z_end in _z_blocks(employee):
            if wb_start < z_start <= wb_end:
                _swap_pair(schedule, employee, z_start - 1, z_start, config)
                break  # 每个工作块只尝试第一个 Z 块


def _can_place_z(schedule: Schedule, employee: Employee, day_index: int, config: SchedulerConfig) -> bool:
    """day_index 能否改为 Z（不动格，试空后校验）。"""
    cell = employee.schedule[day_index]
    if cell.is_locked or cell.is_historical or cell.base_shift not in WORK_SHIFTS or cell.base_shift in Z_FAMILY:
        return False
    if not _demand_ok_removing(schedule, day_index, cell.base_shift, employee.coefficient, config):
        return False
    old = cell.value
    cell.value = None
    try:
        return _can_assign_shift(schedule, employee, day_index, "Z", config)
    finally:
        cell.value = old


def _convert_to_z(schedule: Schedule, employee: Employee, day_index: int, config: SchedulerConfig) -> bool:
    if not (0 <= day_index < len(employee.schedule)):
        return False
    if not _can_place_z(schedule, employee, day_index, config):
        return False
    employee.schedule[day_index].value = "Z"
    return True


def _swap_pair(schedule: Schedule, employee: Employee, i: int, j: int, config: SchedulerConfig) -> bool:
    """行内对调 i/j 两格：双方可动、移除不破需求容差、互换后硬约束成立。"""
    ci, cj = employee.schedule[i], employee.schedule[j]
    if ci.is_locked or ci.is_historical or cj.is_locked or cj.is_historical:
        return False
    si, sj = ci.base_shift, cj.base_shift
    if si not in WORK_SHIFTS or sj not in WORK_SHIFTS or si == sj:
        return False
    if not _demand_ok_removing(schedule, i, si, employee.coefficient, config):
        return False
    if not _demand_ok_removing(schedule, j, sj, employee.coefficient, config):
        return False
    old_i, old_j = ci.value, cj.value
    ci.value, cj.value = None, None
    if _can_assign_shift(schedule, employee, i, sj, config) and _can_assign_shift(
        schedule, employee, j, si, config
    ):
        ci.value, cj.value = sj, si
        return True
    ci.value = old_i
    cj.value = old_j
    return False
```

- [ ] **Step 4: 运行确认通过 + 全 shift 测试回归**

```powershell
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_scheduler.py
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_validators.py
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_utils.py
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_writer.py
```

预期：全 OK。

- [ ] **Step 5: 提交**

```powershell
git add shift/scheduler.py tests/test_shift_scheduler.py
git commit -m "feat(shift): 新增 shape_z_runs——Z 块扩长/合并/贴 OFF 软整型"
```

---

### Task 8: writer 高亮家族化

**Files:**
- Modify: `shift/writer.py:61-80`（`_streak_shifts`、`_highlight_schedule` 分支、导入行）
- Modify: `tests/test_shift_writer.py`（追加用例）

**Interfaces:**
- Consumes: Task 1 的 `D_FAMILY`/`Z_FAMILY`
- Produces: `_streak_shifts(check_id)`：`"08"→D_FAMILY`、`"18"→Z_FAMILY`

- [ ] **Step 1: 追加失败测试**

`tests/test_shift_writer.py` 导入与用例追加：

```python
from utils import D_FAMILY, REST_SHIFT, WORK_SHIFTS, Z_FAMILY  # noqa: E402
from writer import _streak_shifts  # noqa: E402


def test_streak_shifts_family():
    assert _streak_shifts("08") == D_FAMILY
    assert _streak_shifts("18") == Z_FAMILY
    assert _streak_shifts("05") == {REST_SHIFT}
    assert _streak_shifts("04") == WORK_SHIFTS
```

`__main__` 追加调用。

- [ ] **Step 2: 运行确认失败**

```powershell
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_writer.py
```

预期：`test_streak_shifts_family` 失败（`_streak_shifts("08")` 当前返回 `HIGH_LIMIT_SHIFTS`）。

- [ ] **Step 3: 修改 `shift/writer.py`**

导入区加入 `D_FAMILY, Z_FAMILY`。`_streak_shifts` 替换为：

```python
def _streak_shifts(check_id: str) -> set[str]:
    if check_id == "05":
        return {REST_SHIFT}
    if check_id == "08":
        return D_FAMILY
    if check_id == "18":
        return Z_FAMILY
    return WORK_SHIFTS
```

`_highlight_schedule` 中 `if w.check_id in ("04", "05", "08"):` 改为 `if w.check_id in ("04", "05", "08", "18"):`。

- [ ] **Step 4: 运行确认通过**

```powershell
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_writer.py
```

预期：OK。

- [ ] **Step 5: 提交**

```powershell
git add shift/writer.py tests/test_shift_writer.py
git commit -m "feat(shift): 结果高亮按家族着色——check08 D 族、check18 Z 族"
```

---

### Task 9: 模板 xlsx 需求 sheet 加 B/C 行

**Files:**
- Modify: `shift/排班计划.xlsx`、`shift/排班计划 - 主管.xlsx`、`shift/排班计划 - 在线.xlsx`、`shift/排班计划 - 热线.xlsx`（「需求」sheet 末尾追加 B、C 两行标签，需求值留空=0）

**Interfaces:**
- Consumes: 无（一次性数据操作；`reader._read_demands` 对空值按 0 处理，且 B/C 已在 `SHIFT_ORDER` 中有 0 默认键）
- Produces: 带 B/C 行的模板，供用户填写需求

- [ ] **Step 1: 执行一次性脚本**

```powershell
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe -c @"
from openpyxl import load_workbook
names = ['排班计划.xlsx', '排班计划 - 主管.xlsx', '排班计划 - 在线.xlsx', '排班计划 - 热线.xlsx']
for name in names:
    path = 'shift/' + name
    wb = load_workbook(path)
    ws = wb['需求']
    row = ws.max_row + 1
    ws.cell(row, 1, 'B')
    ws.cell(row + 1, 1, 'C')
    wb.save(path)
    print(name, '->', ws.max_row, 'rows')
"@
```

预期：4 个文件各打印行数（各 +2）。

- [ ] **Step 2: 验证**

```powershell
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe -c @"
from openpyxl import load_workbook
for name in ['排班计划.xlsx', '排班计划 - 主管.xlsx', '排班计划 - 在线.xlsx', '排班计划 - 热线.xlsx']:
    wb = load_workbook('shift/' + name)
    ws = wb['需求']
    labels = [str(ws.cell(r, 1).value) for r in range(2, ws.max_row + 1)]
    assert 'B' in labels and 'C' in labels, (name, labels)
    print(name, labels)
"@
```

预期：每个模板 labels 都含 `B`、`C`，无 assert 失败。

- [ ] **Step 3: 提交**

```powershell
git add shift/*.xlsx
git commit -m "chore(shift): 排班计划模板需求 sheet 增加 B/C 班行"
```

---

### Task 10: 全流程冒烟测试 + 收尾

**Files:**
- Create: `tests/test_shift_pipeline.py`

**Interfaces:**
- Consumes: 全部前序任务（reader → run_scheduler → validate → write 完整链路 + Task 9 模板）

- [ ] **Step 1: 写冒烟测试**

创建 `tests/test_shift_pipeline.py`：

```python
# -*- coding: utf-8 -*-
"""shift 全流程冒烟：在线模板注入 B/C 需求 → 排班 → 校验无 ERROR。"""
import os
import shutil
import sys
import time

from openpyxl import load_workbook

_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _ROOT)
sys.path.insert(0, os.path.join(_ROOT, "shift"))

from reader import read_schedule  # noqa: E402
from scheduler import SchedulerConfig, run_scheduler  # noqa: E402
from validators import validate_schedule  # noqa: E402
from writer import write_schedule  # noqa: E402

_TMP = os.path.join(_ROOT, "tests", ".test_tmp")


def test_pipeline_smoke():
    os.makedirs(_TMP, exist_ok=True)
    src = os.path.join(_ROOT, "shift", "排班计划 - 在线.xlsx")
    stamp = time.time_ns()
    work = os.path.join(_TMP, f"smoke_{stamp}.xlsx")
    out = os.path.join(_TMP, f"smoke_out_{stamp}.xlsx")
    shutil.copyfile(src, work)

    wb = load_workbook(work)
    ws = wb["需求"]
    for row in range(2, ws.max_row + 1):
        label = str(ws.cell(row, 1).value or "").strip().upper()
        if label in ("B", "C"):
            for col in range(2, ws.max_column + 1):
                ws.cell(row, col, 2)
    wb.save(work)

    schedule = read_schedule(work)
    config = SchedulerConfig(preset_rest_days=8, z_min_consecutive=2, z_max_consecutive=3)
    run_scheduler(schedule, config)
    schedule.warnings.clear()
    validate_schedule(schedule, config)
    write_schedule(schedule, out)

    errors = [w for w in schedule.warnings if w.severity == "ERROR"]
    assert not errors, [f"{w.check_id}{w.employee}{w.date}: {w.message}" for w in errors[:10]]

    bases = [c.base_shift for e in schedule.employees for c in e.schedule]
    assert "B" in bases, "B 需求>0 应有排班产出"
    assert os.path.isfile(out)


if __name__ == "__main__":
    test_pipeline_smoke()
    print("test_shift_pipeline OK")
```

- [ ] **Step 2: 运行冒烟**

```powershell
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_shift_pipeline.py
```

预期：`test_shift_pipeline OK`。若 ERROR 断言失败，按打印的 check_id 逐条排查（属实现 bug 则修对应模块后重跑，不得放宽断言）。

- [ ] **Step 3: 全量测试回归**

```powershell
$env:PYTHONIOENCODING="utf-8"; Get-ChildItem tests\test_*.py | ForEach-Object { .\.venv\Scripts\python.exe $_.FullName }
```

预期：全部 OK（含 collector/dashboard 等既有测试）。

- [ ] **Step 4: CLI 手工验证一次（可选但推荐）**

```powershell
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe shift\main.py "shift\排班计划 - 在线.xlsx" "$env:TEMP\smoke_cli.xlsx"
```

预期：打印「已输出」「验证信息: ERROR 0 / WARN n / INFO n」，无异常栈。

- [ ] **Step 5: 提交**

```powershell
git add tests/test_shift_pipeline.py
git commit -m "test(shift): B/C+Z 家族重构全流程冒烟测试"
```
