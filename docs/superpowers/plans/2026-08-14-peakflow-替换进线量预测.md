# PeakFlow 替换进线量预测 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 PeakFlow 预测模块迁移到 AutoWFM 的 `peakflow/` 包，替换旧的 `collector/forecast.py`，输出 Excel + HTML 看板。

**Architecture:** PeakFlow 代码以 `peakflow/` 包平级迁移到 AutoWFM 根目录，通过 `config.yaml` 读取配置，`manager.py` 直接调用 `peakflow.main.run_forecast()`。Dashboard 看板保持不变（继续读 `预估流入量.csv`）。

**Tech Stack:** Python 3.14, numpy, pandas, openpyxl（移除 statsmodels 依赖）

## Global Constraints

- 配置文件路径: `D:\PythonProject\AutoWFM\config.yaml`
- PeakFlow 数据源: `D:\PythonProject\AutoTableau\downloads\YYYY-MM-DD\`
- 输出目录: `output/YYYY-MM-DD/`
- 预测固定 30 天
- 每天 09:30 自动运行（AutoTableau 09:00 完成后）
- 删除 21:05 次日差异对比 + 企微告警
- Dashboard 不变，继续读 `预估流入量.csv`

---

### Task 1: 创建 peakflow 包骨架 + config.py

**Files:**
- Create: `peakflow/__init__.py`
- Create: `peakflow/config.py`
- Test: `tests/test_peakflow_config.py`

**Interfaces:**
- Consumes: `config.yaml` 中的 `forecast` 段
- Produces: `config.DATA_DIR`, `config.OUTPUT_DIR`, `config.HORIZON` 等模块级常量，与其他 task 共享

- [ ] **Step 1: 创建 `peakflow/__init__.py`**

```python
# PeakFlow — 客服中心 30 天咨询量预测模块
```

- [ ] **Step 2: 创建 `peakflow/config.py`**

```python
"""PeakFlow 配置 — 从 AutoWFM config.yaml 的 forecast 段读取。"""
from __future__ import annotations
from pathlib import Path
import yaml

BASE_DIR = Path(__file__).resolve().parent.parent
CFG_PATH = BASE_DIR / "config.yaml"

def _load_forecast_cfg():
    with open(CFG_PATH, "r", encoding="utf-8") as f:
        cfg = yaml.safe_load(f)
    return cfg.get("forecast", {})

_fc = _load_forecast_cfg()

DATA_DIR = BASE_DIR / "data"
OUTPUT_DIR = BASE_DIR / _fc.get("output_dir", "output")

# 取数
AUTO_TABLEAU_DIR = Path(_fc.get("auto_tableau_dir", r"D:\PythonProject\AutoTableau"))
FETCH_FILES = _fc.get("fetch_files", ["在线各类用户.csv", "热线各类用户.csv"])
FETCH_MAX_AGE_DAYS = int(_fc.get("fetch_max_age_days", 2))

# 业务常量
CLIENT_TYPES = ["M1", "M2-M3", "M3+", "购买过权益卡且未逾期",
                "behind_30", "over_30", "repay_3in", "repay_3out"]
TOTAL_TYPE = "合计"

# 模型参数
HORIZON = int(_fc.get("horizon", 30))
MIN_HISTORY = int(_fc.get("min_history", 28))
TREND_FIT_DAYS = int(_fc.get("trend_fit_days", 14))
RATIO_WINDOW = int(_fc.get("ratio_window", 14))
BACKTEST_WINDOW = int(_fc.get("backtest_window", 14))
SIGMA_K = float(_fc.get("sigma_k", 1.0))
TREND_DAMP = float(_fc.get("trend_damp", 0.0))
```

- [ ] **Step 3: 创建测试 `tests/test_peakflow_config.py`**

```python
from peakflow import config


def test_client_types_expected():
    assert len(config.CLIENT_TYPES) == 8
    assert config.CLIENT_TYPES[0] == "M1"
    assert config.CLIENT_TYPES[7] == "repay_3out"


def test_horizon():
    assert config.HORIZON == 30
    assert config.BACKTEST_WINDOW <= config.MIN_HISTORY


def test_paths():
    assert config.DATA_DIR.name == "data"
    assert config.OUTPUT_DIR.name == "output"
```

- [ ] **Step 4: 运行测试验证**

```bash
.\.venv\Scripts\python.exe tests\test_peakflow_config.py
```

预期: 无报错通过（或提示缺少 yaml 依赖则先 `pip install pyyaml`）

- [ ] **Step 5: Commit**

```bash
git add peakflow/__init__.py peakflow/config.py tests/test_peakflow_config.py
git commit -m "feat(peakflow): add peakflow package skeleton with config from config.yaml"
```

---

### Task 2: 迁移 models.py（趋势+周季节分解算法）

**Files:**
- Create: `peakflow/models.py`
- Test: `tests/test_peakflow_models.py`

**Interfaces:**
- Consumes: `peakflow.config` (TREND_FIT_DAYS, TREND_DAMP, RATIO_WINDOW)
- Produces: `forecast_client_volume(series, future_dates) -> np.ndarray`, `forecast_ratio(series, future_dates) -> np.ndarray`, `mean_recent_ratio(series, window=None) -> float`

- [ ] **Step 1: 从 PeakFlow 复制 `models.py`，改 import 路径**

将 `D:\PythonProject\PeakFlow\src\models.py` 复制到 `peakflow/models.py`，将 `from . import config` 改为 `from peakflow import config`。

完整内容（同 PeakFlow 原文件，仅改 import）：

```python
from __future__ import annotations

import numpy as np
import pandas as pd

from peakflow import config


def _trend_tail(series: pd.Series) -> pd.Series:
    """7 日中心滑动平均趋势，边缘用最近值填充。"""
    trend = series.astype(float).rolling(7, center=True).mean()
    return trend.ffill().bfill()


def _seasonal_index(series: pd.Series, trend: pd.Series) -> pd.Series:
    """按星期几(0-6)求 序列-趋势 的均值。"""
    resid = series.astype(float) - trend
    tmp = pd.DataFrame({"v": resid.values, "wd": resid.index.weekday})
    return tmp.groupby("wd")["v"].mean()


def _extrapolate(trend: pd.Series, horizon: int) -> np.ndarray:
    """对趋势尾部 TREND_FIT_DAYS 天线性拟合外推 horizon 步。"""
    n = len(trend)
    fit = min(config.TREND_FIT_DAYS, n)
    tail = trend.iloc[-fit:].values.astype(float)
    x = np.arange(fit, dtype=float)
    slope, intercept = np.polyfit(x, tail, 1)
    xf = np.arange(fit, fit + horizon, dtype=float)
    if config.TREND_DAMP > 0:
        steps = xf - (fit - 1)
        eff = slope * (config.TREND_DAMP ** steps)
        out = np.empty(horizon)
        out[0] = intercept + slope * (fit - 1) + eff[0]
        for i in range(1, horizon):
            out[i] = out[i - 1] + eff[i]
        return out
    return intercept + slope * xf


def forecast_client_volume(series: pd.Series, future_dates: list) -> np.ndarray:
    """客户量：趋势+周季节分解外推。返回长度=len(future_dates)、值>=0 的数组。"""
    s = series.dropna()
    if len(s) < 21:
        raise ValueError(f"客户量历史不足({len(s)} 天)，需要至少 21 天")
    s = s.sort_index()
    trend = _trend_tail(s)
    sidx = _seasonal_index(s, trend)
    tf = _extrapolate(trend, len(future_dates))
    out = np.array([tf[i] + sidx.get(future_dates[i].weekday(), 0.0)
                    for i in range(len(future_dates))])
    return np.maximum(out, 0.0)


def forecast_ratio(series: pd.Series, future_dates: list) -> np.ndarray:
    """咨询占比：对 log(r) 做趋势+周季节分解外推，再 exp。返回 (0,1] 数组。"""
    s = series.dropna()
    s = s[s > 0]
    if len(s) < 21:
        last = float(s.iloc[-1]) if len(s) else 0.0
        return np.clip(np.full(len(future_dates), last), 1e-9, 1.0)
    s = s.sort_index()
    ls = np.log(s)
    trend = _trend_tail(ls)
    sidx = _seasonal_index(ls, trend)
    tf = _extrapolate(trend, len(future_dates))
    out = np.exp(np.array([tf[i] + sidx.get(future_dates[i].weekday(), 0.0)
                           for i in range(len(future_dates))]))
    return np.clip(out, 1e-9, 1.0)


def mean_recent_ratio(series: pd.Series, window: int | None = None) -> float:
    """最近 window 天占比均值，clamp [0,1]。"""
    window = window or config.RATIO_WINDOW
    s = series.dropna()
    s = s[s > 0]
    if len(s) == 0:
        return 0.0
    v = float(s.iloc[-window:].mean())
    return float(np.clip(v, 0.0, 1.0))
```

- [ ] **Step 2: 复制测试文件，改 import**

将 `D:\PythonProject\PeakFlow\tests\test_models.py` 和 `D:\PythonProject\PeakFlow\tests\helpers.py` 复制到 `tests/`，修改 import：
- `from src import models` → `from peakflow import models`
- `from src import config` → `from peakflow import config`
- `from tests.helpers` 保持不变

- [ ] **Step 3: 运行测试**

```bash
.\.venv\Scripts\python.exe tests\test_peakflow_models.py
```

预期: 所有测试 PASS

- [ ] **Step 4: Commit**

```bash
git add peakflow/models.py tests/test_peakflow_models.py tests/helpers.py
git commit -m "feat(peakflow): migrate models.py - trend+seasonal decomposition"
```

---

### Task 3: 迁移 loader.py（CSV 解析）

**Files:**
- Create: `peakflow/loader.py`
- Test: `tests/test_peakflow_loader.py`

**Interfaces:**
- Consumes: `peakflow.config` (CLIENT_TYPES, TOTAL_TYPE)
- Produces: `load_channel_data(path) -> pd.DataFrame`, `detect_encoding(path) -> str`

- [ ] **Step 1: 复制 `loader.py`，改 import**

将 `D:\PythonProject\PeakFlow\src\loader.py` 复制到 `peakflow/loader.py`，将 `from . import config` 改为 `from peakflow import config`。

- [ ] **Step 2: 复制 `tests/test_loader.py`，改 import**

将 `from src import loader` → `from peakflow import loader`

- [ ] **Step 3: 运行测试**

```bash
.\.venv\Scripts\python.exe -m pytest tests\test_peakflow_loader.py -v
```

- [ ] **Step 4: Commit**

```bash
git add peakflow/loader.py tests/test_peakflow_loader.py
git commit -m "feat(peakflow): migrate loader.py - CSV parsing"
```

---

### Task 4: 迁移 fetch.py（AutoTableau 数据同步）

**Files:**
- Create: `peakflow/fetch.py`
- Test: `tests/test_peakflow_fetch.py`

**Interfaces:**
- Consumes: `peakflow.config` (AUTO_TABLEAU_DIR, FETCH_FILES, FETCH_MAX_AGE_DAYS, DATA_DIR)
- Produces: `sync_from_autotableau(root, dest, files) -> Path`, `latest_download_dir(root) -> Path`

- [ ] **Step 1: 复制 `fetch.py`，改 import**

将 `from . import config` → `from peakflow import config`

- [ ] **Step 2: 复制 `tests/test_fetch.py`，改 import**

将 `from src import fetch` → `from peakflow import fetch`

- [ ] **Step 3: 运行测试**

```bash
.\.venv\Scripts\python.exe -m pytest tests\test_peakflow_fetch.py -v
```

- [ ] **Step 4: Commit**

```bash
git add peakflow/fetch.py tests/test_peakflow_fetch.py
git commit -m "feat(peakflow): migrate fetch.py - AutoTableau sync"
```

---

### Task 5: 迁移 forecast.py（三档区间预测）

**Files:**
- Create: `peakflow/forecast.py`
- Test: `tests/test_peakflow_forecast.py`

**Interfaces:**
- Consumes: `peakflow.config` (CLIENT_TYPES, BACKTEST_WINDOW, SIGMA_K), `peakflow.models`
- Produces: `point_forecast(history_df, future_dates) -> pd.DataFrame`, `total_by_date(forecast_df) -> pd.DataFrame`, `weekly_summary(total_df) -> pd.DataFrame`, `backtest_sigma(history_df) -> dict`, `three_band_forecast(history_df, future_dates) -> pd.DataFrame`

- [ ] **Step 1: 复制 `forecast.py`，改 import**

将 `from . import config` → `from peakflow import config`，`from .models import ...` → `from peakflow.models import ...`

- [ ] **Step 2: 复制 `tests/test_forecast.py`，改 import**

将 `from src import config, forecast` → `from peakflow import config, forecast`，`from tests.helpers` 保持不变

- [ ] **Step 3: 运行测试**

```bash
.\.venv\Scripts\python.exe -m pytest tests\test_peakflow_forecast.py -v
```

- [ ] **Step 4: Commit**

```bash
git add peakflow/forecast.py tests/test_peakflow_forecast.py
git commit -m "feat(peakflow): migrate forecast.py - three-band prediction"
```

---

### Task 6: 迁移 report.py（Excel 输出）

**Files:**
- Create: `peakflow/report.py`
- Test: `tests/test_peakflow_report.py`

**Interfaces:**
- Consumes: `peakflow.config`, `peakflow.forecast`
- Produces: `write_report(online_df, hotline_df, sigmas, meta, out_path) -> Path`

- [ ] **Step 1: 复制 `report.py`，改 import**

将 `from . import config` → `from peakflow import config`，`from .forecast import ...` → `from peakflow.forecast import ...`

- [ ] **Step 2: 复制 `tests/test_report.py`，改 import**

将 `from src import config, report` → `from peakflow import config, report`，`from src.forecast import ...` → `from peakflow.forecast import ...`

- [ ] **Step 3: 运行测试**

```bash
.\.venv\Scripts\python.exe -m pytest tests\test_peakflow_report.py -v
```

- [ ] **Step 4: Commit**

```bash
git add peakflow/report.py tests/test_peakflow_report.py
git commit -m "feat(peakflow): migrate report.py - Excel output"
```

---

### Task 7: 迁移 dashboard.py + HTML 模板

**Files:**
- Create: `peakflow/dashboard.py`
- Create: `peakflow/dashboard_template.html`
- Test: `tests/test_peakflow_dashboard.py`

**Interfaces:**
- Consumes: `peakflow.config`, `peakflow.forecast`
- Produces: `build_dashboard_data(histories, forecasts, meta) -> dict`, `write_dashboard(data, out_path) -> Path`

- [ ] **Step 1: 复制 `dashboard.py` 和 `dashboard_template.html`**

```bash
copy D:\PythonProject\PeakFlow\src\dashboard.py D:\PythonProject\AutoWFM\peakflow\dashboard.py
copy D:\PythonProject\PeakFlow\src\dashboard_template.html D:\PythonProject\AutoWFM\peakflow\dashboard_template.html
```

修改 `dashboard.py` 的 import：`from . import config` → `from peakflow import config`

修改 `dashboard.py` 第 11 行 `_TEMPLATE` 路径：`Path(__file__).with_name("dashboard_template.html")` 保持不变（已经是相对路径，正确）

- [ ] **Step 2: 复制 `tests/test_dashboard.py`，改 import**

```bash
copy D:\PythonProject\PeakFlow\tests\test_dashboard.py D:\PythonProject\AutoWFM\tests\test_peakflow_dashboard.py
```

修改 import：`from src import dashboard` → `from peakflow import dashboard`

- [ ] **Step 3: 运行测试**

```bash
.\.venv\Scripts\python.exe -m pytest tests\test_peakflow_dashboard.py -v
```

- [ ] **Step 4: Commit**

```bash
git add peakflow/dashboard.py peakflow/dashboard_template.html tests/test_peakflow_dashboard.py
git commit -m "feat(peakflow): migrate dashboard.py + HTML template"
```

---

### Task 8: 创建 main.py（入口）

**Files:**
- Create: `peakflow/main.py`
- Test: `tests/test_peakflow_main.py`

**Interfaces:**
- Consumes: `peakflow.config`, `peakflow.fetch`, `peakflow.loader`, `peakflow.forecast`, `peakflow.report`, `peakflow.dashboard`
- Produces: `run_forecast(fetch=False, out_dir=None) -> Path` — 供 manager.py 调用

- [ ] **Step 1: 创建 `peakflow/main.py`**

```python
from __future__ import annotations

import argparse
import datetime as dt
import sys
from pathlib import Path

import pandas as pd

from peakflow import config, dashboard, loader, report
from peakflow import fetch as fetch_mod
from peakflow.forecast import backtest_sigma, three_band_forecast

CHANNEL_FILES = [("在线", "在线各类用户.csv"), ("热线", "热线各类用户.csv")]


def run_forecast(fetch: bool = False, out_dir: Path | None = None) -> Path:
    if fetch:
        src = fetch_mod.sync_from_autotableau()
        print(f"已同步数据，来源: {src}")
    if out_dir is None:
        out_dir = config.OUTPUT_DIR
    histories, results, sigmas = {}, {}, {}
    history_days = 0
    for channel, fname in CHANNEL_FILES:
        path = config.DATA_DIR / fname
        hist = loader.load_channel_data(path)
        histories[channel] = hist
        history_days = int(hist["date"].nunique())
        if history_days < config.MIN_HISTORY:
            print(f"警告: {channel} 历史仅 {history_days} 天 (< {config.MIN_HISTORY})，预测可能不稳")
        last_date = hist["date"].max()
        future_dates = [last_date + dt.timedelta(days=i)
                        for i in range(1, config.HORIZON + 1)]
        results[channel] = three_band_forecast(hist, future_dates)
        sigmas[channel] = backtest_sigma(hist)
        print(f"{channel}: 预测完成，数据截止 {last_date.date()}，未来 {len(future_dates)} 天")
    meta = {
        "生成时间": dt.datetime.now().strftime("%Y-%m-%d %H:%M:%S"),
        "数据截止日期": str(last_date.date()),
        "历史天数": history_days,
        "预测天数": config.HORIZON,
        "趋势拟合天数": config.TREND_FIT_DAYS,
        "转人工占比窗口": config.RATIO_WINDOW,
        "回测窗口": config.BACKTEST_WINDOW,
        "区间倍数 k": config.SIGMA_K,
        "模型": "趋势+周季节分解外推（客户量/咨询占比），固定转人工占比",
    }
    out_dir = Path(out_dir)
    run_day = dt.date.today().isoformat()
    out_dir = out_dir / run_day
    out_dir.mkdir(parents=True, exist_ok=True)
    out_path = out_dir / f"预测_{dt.date.today():%Y%m%d}_未来{config.HORIZON}天.xlsx"
    report.write_report(results["在线"], results["热线"], sigmas, meta, out_path)
    dashboard_path = out_path.with_suffix(".html")
    dashboard.write_dashboard(
        dashboard.build_dashboard_data(histories, results, meta), dashboard_path
    )
    print("网页:", dashboard_path)
    return out_path


def main(argv=None) -> int:
    ap = argparse.ArgumentParser(description="客服中心 30 天在线/热线咨询量预测")
    ap.add_argument("--fetch", action="store_true",
                    help="先从 AutoTableau 同步最新数据，再预测")
    args = ap.parse_args(argv)
    out = run_forecast(fetch=args.fetch)
    print("完成:", out)
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: 复制 `tests/test_main.py`，改 import**

```bash
copy D:\PythonProject\PeakFlow\tests\test_main.py D:\PythonProject\AutoWFM\tests\test_peakflow_main.py
```

修改 import：`from src import main` → `from peakflow import main`，`monkeypatch.setattr("src.config.DATA_DIR", ...)` → `monkeypatch.setattr("peakflow.config.DATA_DIR", ...)`

- [ ] **Step 3: 运行测试**

```bash
.\.venv\Scripts\python.exe -m pytest tests\test_peakflow_main.py -v
```

- [ ] **Step 4: Commit**

```bash
git add peakflow/main.py tests/test_peakflow_main.py
git commit -m "feat(peakflow): add main.py entry point"
```

---

### Task 9: 修改 config.yaml

**Files:**
- Modify: `config.yaml` — 替换 forecast 配置段

**Interfaces:**
- Consumes: 无
- Produces: `forecast` 配置段供 `peakflow/config.py` 读取

- [ ] **Step 1: 修改 `config.yaml` 的 forecast 段**

替换第 45-52 行：

```yaml
forecast:
  # 数据源
  auto_tableau_dir: "D:\\PythonProject\\AutoTableau"
  fetch_files: ["在线各类用户.csv", "热线各类用户.csv"]
  fetch_max_age_days: 2
  
  # 模型参数
  horizon: 30
  min_history: 28
  trend_fit_days: 14
  ratio_window: 14
  backtest_window: 14
  sigma_k: 1.0
  trend_damp: 0.0
  
  # 输出
  output_dir: "output"
```

- [ ] **Step 2: 验证配置可被加载**

```bash
.\.venv\Scripts\python.exe -c "from peakflow import config; print('HORIZON:', config.HORIZON); print('DATA_DIR:', config.DATA_DIR)"
```

预期输出: `HORIZON: 30` / `DATA_DIR: D:\PythonProject\AutoWFM\data`

- [ ] **Step 3: Commit**

```bash
git add config.yaml
git commit -m "feat(config): update forecast section for PeakFlow"
```

---

### Task 10: 修改 manager.py

**Files:**
- Modify: `manager.py`

**Interfaces:**
- Consumes: `peakflow.main.run_forecast`
- Produces: Tkinter 预测页面调用新预测逻辑

- [ ] **Step 1: 删除 `_check_forecast` 调用**

在 `_refresh` 方法中（第 579 行），删除 `self._check_forecast(now)` 这一行。

- [ ] **Step 2: 删除 `_check_forecast` 和 `_run_forecast_diff` 方法**

删除第 637-657 行（`_check_forecast` 和 `_run_forecast_diff` 两个方法）。

- [ ] **Step 3: 删除 `_forecast_date` 和 `_forecast_running` 初始化**

在 `__init__` 中（第 452-453 行），删除：
```python
self._forecast_date = ""
self._forecast_running = False
```

- [ ] **Step 4: 修改 `_build_forecast_page` 方法**

将第 766-782 行替换为：

```python
    # ---- 进线量预测(手动触发;定时 09:30 由 Windows 计划任务自动跑)----
    def _build_forecast_page(self, page: tk.Frame) -> None:
        top = tk.Frame(page, padx=10, pady=8)
        top.pack(fill=tk.X)
        self.btn_forecast = tk.Button(top, text="运行预测(30天)", width=14, command=self._run_forecast)
        self.btn_forecast.pack(side=tk.LEFT, padx=6)
        self.forecast_status_var = tk.StringVar(value="就绪")
        tk.Label(top, textvariable=self.forecast_status_var, fg="#555555").pack(side=tk.LEFT, padx=10)
        self.forecast_box = scrolledtext.ScrolledText(page, wrap=tk.WORD, font=("Consolas", 10))
        self.forecast_box.pack(fill=tk.BOTH, expand=True, padx=10, pady=(4, 10))
        self.forecast_box.configure(state=tk.DISABLED)
        self._set_forecast_text(
            "点「运行预测(30天)」从 AutoTableau 同步最新数据并预测未来 30 天。\n"
            "每天 09:30 Windows 计划任务自动运行。\n"
            "输出: output/YYYY-MM-DD/预测_YYYYMMDD_未来30天.xlsx + .html")
```

- [ ] **Step 5: 修改 `_run_forecast` 方法**

将第 793-818 行替换为：

```python
    def _run_forecast(self) -> None:
        self._set_forecast_status("运行中...")
        self._set_forecast_text("正在同步数据并预测，请稍候...")
        self.btn_forecast.configure(state=tk.DISABLED)

        def worker():
            try:
                from peakflow.main import run_forecast
                out_path = run_forecast(fetch=True)
                summary = self._forecast_summary(out_path)
                self.root.after(0, self._on_forecast_done, summary, None)
            except Exception as exc:
                log.exception("手动预测失败")
                self.root.after(0, self._on_forecast_done, "", exc)

        threading.Thread(target=worker, daemon=True, name="forecast").start()
```

- [ ] **Step 6: 修改 `_on_forecast_done` 方法**

将第 820-830 行替换为：

```python
    def _on_forecast_done(self, summary: str, err: Exception | None) -> None:
        self.btn_forecast.configure(state=tk.NORMAL)
        if err is not None:
            self._set_forecast_status("失败")
            self._set_forecast_text(
                f"预测失败: {err}\n\n常见原因:\n"
                "- AutoTableau 下载目录无最新数据\n"
                "- data/ 缺少 CSV 文件\n"
                "- 历史数据不足（需 ≥28 天）\n请查看 logs/manager.log")
        else:
            self._set_forecast_status("完成")
            self._set_forecast_text(summary)
```

- [ ] **Step 7: 修改 `_forecast_summary` 静态方法**

将第 832-850 行替换为：

```python
    @staticmethod
    def _forecast_summary(out_path) -> str:
        """展示 Excel 路径和基本信息。"""
        from pathlib import Path
        path = Path(out_path)
        lines = [
            f"预测完成！",
            f"Excel: {path}",
            f"HTML:  {path.with_suffix('.html')}",
            "",
            "用 Excel 打开查看总览/分类型明细/回测误差/运行说明。",
            "用浏览器打开 HTML 查看可视化图表。",
        ]
        return "\n".join(lines)
```

- [ ] **Step 8: 运行 manager.py 冒烟测试**

```bash
.\.venv\Scripts\python.exe -c "from manager import ManagerUI; print('import OK')"
```

预期: `import OK`

- [ ] **Step 9: Commit**

```bash
git add manager.py
git commit -m "refactor(manager): switch forecast to peakflow, remove 21:05 diff"
```

---

### Task 11: 删除旧代码

**Files:**
- Delete: `collector/forecast.py`
- Modify: `collector/notify.py` — 删除 `forecast_at()` 函数及调用
- Modify: `collector/scheduler.py` — 无 forecast diff 相关代码（经确认无需修改）

- [ ] **Step 1: 删除 `collector/forecast.py`**

```bash
git rm collector/forecast.py
```

- [ ] **Step 2: 修改 `collector/notify.py`**

删除第 41-48 行的 `forecast_at()` 函数，以及第 113-114 行对其的调用。

在 `collector/notify.py` 中，找到 `build_message()` 函数（约第 100-120 行），删除 `f_hot = forecast_at(...)` 和 `f_ol = forecast_at(...)` 这两个调用，以及消息模板中 `>预测量:` 相关行。

具体修改 `build_message()` 函数：将使用 `forecast_at` 的 message 模板改为不包含预测量。

- [ ] **Step 3: 验证无残留引用**

```bash
rg "from collector.forecast|from collector import forecast|import collector.forecast" --include="*.py" D:\PythonProject\AutoWFM
```

预期: 无输出

- [ ] **Step 4: Commit**

```bash
git add collector/forecast.py collector/notify.py
git commit -m "refactor: remove old collector/forecast.py and forecast_at()"
```

---

### Task 12: 迁移测试并全量验证

**Files:**
- Create: `tests/test_peakflow_config.py` (Task 1 已创建)
- Create: `tests/test_peakflow_models.py` (Task 2 已创建)
- Create: `tests/helpers.py` (Task 2 已创建)
- Create: `tests/test_peakflow_loader.py` (Task 3 已创建)
- Create: `tests/test_peakflow_fetch.py` (Task 4 已创建)
- Create: `tests/test_peakflow_forecast.py` (Task 5 已创建)
- Create: `tests/test_peakflow_report.py` (Task 6 已创建)
- Create: `tests/test_peakflow_dashboard.py` (Task 7 已创建)
- Create: `tests/test_peakflow_main.py` (Task 8 已创建)

- [ ] **Step 1: 运行全部 peakflow 测试**

```bash
.\.venv\Scripts\python.exe -m pytest tests/test_peakflow_*.py -v
```

预期: 所有测试 PASS

- [ ] **Step 2: 运行原有测试（确保无回归）**

```bash
.\.venv\Scripts\python.exe -m pytest tests/test_*.py -v --ignore=tests/test_peakflow_*.py -k "not forecast"
```

- [ ] **Step 3: 修复任何失败**

- [ ] **Step 4: Commit**

```bash
git add tests/
git commit -m "test(peakflow): add all peakflow tests"
```

---

### Task 13: 更新 Windows 计划任务

**Files:**
- 无代码变更，操作 Windows 任务计划

- [ ] **Step 1: 更新计划任务**

```powershell
# 删除旧任务（如果存在）
Unregister-ScheduledTask -TaskName "PeakFlow_Daily" -Confirm:$false -ErrorAction SilentlyContinue

# 创建新任务
$action = New-ScheduledTaskAction -Execute "D:\PythonProject\AutoWFM\.venv\Scripts\pythonw.exe" `
    -Argument "-m peakflow.main --fetch" `
    -WorkingDirectory "D:\PythonProject\AutoWFM"
$trigger = New-ScheduledTaskTrigger -Daily -At "09:30"
$principal = New-ScheduledTaskPrincipal -UserId "liyuting" -LogonType Interactive
Register-ScheduledTask -TaskName "AutoWFM_Forecast" -Action $action -Trigger $trigger -Principal $principal
```

- [ ] **Step 2: 验证任务**

```powershell
Get-ScheduledTask -TaskName "AutoWFM_Forecast" | Format-List TaskName, State
```

- [ ] **Step 3: 手动触发一次测试**

```powershell
Start-ScheduledTask -TaskName "AutoWFM_Forecast"
```

检查 `output/` 目录是否生成 Excel 和 HTML。

- [ ] **Step 4: 记录完成**

无 git commit（系统配置变更）。

---

## 任务依赖图

```
Task 1 (config) ──┬── Task 2 (models)
                  ├── Task 3 (loader)
                  ├── Task 4 (fetch)
                  ├── Task 5 (forecast) ← depends on Task 2
                  ├── Task 6 (report) ← depends on Task 5
                  ├── Task 7 (dashboard) ← depends on Task 5
                  ├── Task 8 (main) ← depends on Tasks 4,5,6,7
                  └── Task 9 (config.yaml) ← depends on Task 1
Task 10 (manager.py) ← depends on Task 8
Task 11 (delete old) ← independent (can run anytime after Task 8)
Task 12 (tests) ← depends on Tasks 1-8
Task 13 (scheduled task) ← depends on Task 8
```